package UI.Panels;

import javax.swing.*;

public class HomePanel extends JPanel {

    private JLabel label;
    public HomePanel() {
        label = new JLabel("Home panel");
        this.add(label);
    }
}
